feat(desktop): backup/restore SQLite y sincronizacion atribuida al doctor activo

- BackupController soporta el driver sqlite: dump SQL portable + copia cruda del archivo en el mismo ZIP; la restauracion detecta la BD SQLite y la reemplaza de forma atomica guardando una copia previa automatica (pre_restore_*.sqlite); el flujo SQL sigue funcionando como antes
- correccion en create(): ZipArchive lee los archivos perezosamente en close(), se cerraba antes de borrar la copia cruda -> backup roto en sqlite
- el login web escribe storage/app/sync/active-user.txt y el logout lo limpia; el ciclo de sync (proceso CLI) lee ese marcador para atribuir sync_states y la actividad al doctor con sesion activa en lugar de la cuenta de admin configurada (localSyncUserId con fallback a config sync.email)